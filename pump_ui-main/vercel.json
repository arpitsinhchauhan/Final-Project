{
    "version": 2,
    "builds": [
        {
            "src": "angular.json",
            "use": "@vercel/angular"
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "http://localhost:8080/$1"
        },
        {
            "src": "/(.*)",
            "dest": "/index.html"
        }
    ]
}